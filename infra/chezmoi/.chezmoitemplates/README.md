# chezmoi Templates

Shared template fragments for user-state rendering will live here once migration begins.

Foundation phase intentionally leaves this directory without active templates so ownership and layout can settle before hot-path migration starts.
