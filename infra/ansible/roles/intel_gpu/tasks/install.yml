---
- name: Install Intel GPU packages (legacy override; default is manifest via packages role)
  ansible.builtin.package:
    name: "{{ intel_gpu_packages_by_platform[host_platform_family] }}"
    state: present
  when: intel_gpu_packages_by_platform[host_platform_family] | default([]) | length > 0
