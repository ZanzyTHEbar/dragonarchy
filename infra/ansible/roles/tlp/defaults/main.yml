---
tlp_enabled: true

# Packages: deps.manifest.toml (laptop_power_tlp) via packages role.
tlp_packages_by_platform:
  arch: []

tlp_conflict_services:
  - power-profiles-daemon.service

tlp_masked_services:
  - systemd-rfkill.service
  - systemd-rfkill.socket

tlp_host_source_dir: "{{ playbook_dir | dirname | dirname | dirname }}/hosts"
