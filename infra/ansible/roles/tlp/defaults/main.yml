---
tlp_enabled: true

tlp_packages_by_platform:
  arch:
    - tlp
    - tlp-rdw

tlp_conflict_services:
  - power-profiles-daemon.service

tlp_masked_services:
  - systemd-rfkill.service
  - systemd-rfkill.socket

tlp_config_src_by_host:
  firedragon: hosts/firedragon/01-firedragon.conf
  goldendragon: hosts/goldendragon/01-goldendragon.conf
