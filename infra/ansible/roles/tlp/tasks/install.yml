---
- name: Install TLP packages (legacy override; default is manifest via packages role)
  ansible.builtin.package:
    name: "{{ tlp_packages_by_platform[host_platform_family] }}"
    state: present
  when: tlp_packages_by_platform[host_platform_family] | default([]) | length > 0
