[General]
# Enable iwd's built-in network configuration (DHCP/DNS handling).
# This is required for iwd-native clients like `impala` to successfully connect
# when NetworkManager is not managing the Wi-Fi interface.
EnableNetworkConfiguration=true

