[Resolve]
DNS=192.168.0.218 1.1.1.1 8.8.8.8
# Remove or comment out: Domains=~.
# Optional: Add these for better control
# DNSOverTLS=opportunistic  # If you want encrypted DNS where possible
# DNSSEC=no  # Pi-hole handles blocking; avoid conflicts if issues arise