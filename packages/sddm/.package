scope=system

# ⚠️  DEPRECATED for managed hosts: Ansible sddm role now owns SDDM configuration.
# This package is retained for reference and for legacy install.sh usage.
# For new installs, use: ./install --host <hostname>
#
# Replacement: infra/ansible/roles/sddm/

