#!/bin/bash
# Displays a power menu.

show_power_menu() {
  local options=("Lock" "Suspend" "Relaunch WM" "Restart" "Shutdown" "Cancel")
  local choice=$(printf "%s\n" "${options[@]}" | gum choose --header="Power Menu")

  case "$choice" in
  "Lock") hyprlock ;;
  "Suspend") systemctl suspend ;;
  "Relaunch WM") uwsm stop ;;
  "Restart") systemctl reboot ;;
  "Shutdown") systemctl poweroff ;;
  "Cancel") exit 0 ;;
  esac
}

show_power_menu
