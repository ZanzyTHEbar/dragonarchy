# Runtime monitor include stub.
#
# The real runtime-owned file is `monitor.local.conf`, generated by
# `scripts/theme-manager/dynamic-monitors`. This stub ensures the globbed
# source in `monitor.conf` always matches at least one file.
